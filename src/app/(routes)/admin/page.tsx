import { staticMetadata } from "@/lib/seo/metadata";

export const metadata = staticMetadata({
  title: "Admin",
  description: "Admin dasboard DawahPlay",
});

const AddChannelPage = () => {
  return <div>AddChannelPage</div>;
};

export default AddChannelPage;
